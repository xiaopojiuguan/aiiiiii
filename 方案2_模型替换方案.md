# 方案2：模型替换方案 —— 从 YOLO11m-P2 切换到其他检测架构

> 背景：YOLO11m-P2 基线 mAP50=0.208、YOLO11x-P2 mAP50=0.206，AAFM/MFFM/QFL 等模块均无稳定提升。容量和结构模块不是瓶颈，需要考虑**换范式**。
> 日期：2026-07-27

---

## 一、为什么要换模型

| 已尝试 | 结论 |
|---|---|
| YOLO11m-P2 → YOLO11x-P2 | 参数 ×2.8，mAP ↓0.002，容量不是瓶颈 |
| QFL 类别重加权 | mAP 0.197，低于基线 |
| AAFM 抗伪影融合 | 无明显提升 |
| MFFM 多形态融合 | 无明显提升 |

### 已确认的关键信号

从 baseline `results.csv`，分类过拟合远严重于定位：

| Epoch | train/cls_loss | val/cls_loss | cls 比值 | train/box_loss | val/box_loss | box 比值 |
|---|---|---|---|---|---|---|
| 31 (best) | 0.521 | 1.624 | **3.1×** | 0.360 | 0.765 | 2.1× |
| 42 | 0.466 | 3.657 | **7.8×** | 0.338 | 0.759 | 2.2× |

**分类的过拟合程度是定位的 2～4 倍。** 单阶段检测器强迫两个任务共享 neck 特征，分类梯度在干扰定位。但这一点已经通过 cls_loss/box_loss 比值确认，不需要再跑两阶段解耦实验来诊断。

YOLO11 系列共享同一套 TAL 标签分配 + 耦合分类/回归头 + COCO 自然图像预训练。在同一个范式内换大小不会改变这四个核心设计，因此需要换掉其中至少一个。

---

## 二、候选模型总览

| # | 方案 | 换掉了什么 | 环境风险 | 训练耗时 | 核心假设 |
|---|---|---|---|---|---|
| 1 | **RT-DETR** | TAL → 匈牙利匹配，去掉 NMS | 低 | ~6h | 密集同类目标下 NMS 误杀真框，DETR 全局建模更适合纹理微弱的缺陷 |
| 2 | **D-FINE** | 框回归方式 → 细粒度分布精修 | 中 | ~6h | mAP50-95/mAP50=58%，框找得到但贴不准 |
| 3 | **Co-DINO** | 一对一匹配 + 额外挂一对多辅助头 | 高 | ~10h | 探上限：如果最强检测器也只有 ~0.22，确认数据天花板 |
| 4 | **DINOv2 冻结骨干** | COCO 有监督预训练 → 自监督通用特征 | 中 | ~4h | 预训练域差距大（RGB → 灰度工业图）+ 冻结后少参数缓解长尾过拟合 |

---

## 三、方案 1：RT-DETR（首选，立刻跑）

### 3.1 为什么首选

- **零环境成本**：ultralytics 内置 `RTDETR` 类，你已装 8.4.105
- **零数据改动**：直接吃 `tile_dataset.yaml`（标准 YOLO txt 格式）
- **理论匹配**：
  - 一对一匈牙利匹配 → 密集同类目标（麻面麻坑）不会被 NMS 误杀
  - 无 NMS → 省掉 IoU 阈值调节，细长裂纹不会被错误合并
  - 编码器-解码器全局建模 → 强于 YOLO 的局部感受野

### 3.2 速筛训练

```bash
PYTHONUNBUFFERED=1 python -u -c "
from ultralytics import RTDETR
m = RTDETR('rtdetr-l.pt')
m.train(
    data='tile_dataset.yaml', epochs=15, imgsz=1280,
    batch=2, optimizer='AdamW', lr0=1e-4, weight_decay=1e-4,
    warmup_epochs=2, amp=True, cache=False,
    hsv_h=0.0, hsv_s=0.0, hsv_v=0.3,
    degrees=0.0, fliplr=0.5, flipud=0.0, mosaic=0.0,
    project='outputs/checkpoints', name='rtdetr_screen'
)
"
```

关键参数说明：
- `mosaic=0.0`：DETR 系列对 mosaic 不友好，会打乱匈牙利匹配的全局一致性
- `lr0=1e-4`：DETR 学习率通常低于 YOLO

### 3.3 推理注意事项

- RT-DETR 直接输出 300 个 query，按 conf 排序即可，**不走 Soft-NMS**
- 需要在 predict 脚本中加 `--no-nms` 开关

### 3.4 判读标准

| 速筛 mAP50 | 判断 | 下一步 |
|---|---|---|
| > 0.25 | 范式是瓶颈，assignment+NMS 是核心问题 | 上全量数据 + RT-DETR-R101 |
| 0.19–0.23 | 两种完全不同的 assignment 同分 | 强烈指向数据是天花板 |
| < 0.15 | 可能未收敛 | 必须先看 loss 曲线是否还在下降 |

**重要**：DETR 公认收敛慢。15 epoch 对它不公平，**必须看 loss/mAP 曲线是否还在单调上升**——如果还在涨，给 30 epoch 再判读。

### 3.5 如果速筛有效：全量训练

```bash
PYTHONUNBUFFERED=1 python -u -c "
from ultralytics import RTDETR
m = RTDETR('rtdetr-l.pt')
m.train(
    data='tile_dataset.yaml', epochs=60, imgsz=1280,
    batch=2, optimizer='AdamW', lr0=1e-4, weight_decay=1e-4,
    warmup_epochs=3, amp=True,
    hsv_h=0.0, hsv_s=0.0, hsv_v=0.3,
    degrees=0.0, fliplr=0.5, flipud=0.0, mosaic=0.0,
    project='outputs/checkpoints', name='rtdetr_full'
)
"
```

---

## 四、方案 2：D-FINE（条件触发：框贴不准）

### 4.1 为什么

- 在 RT-DETR 基础上，把框回归从"直接回归坐标"换成**细粒度分布精修（Fine-grained Distribution Refinement）**
- baseline 的 mAP50-95/mAP50 = 0.121/0.208 = 58%，这个比例偏低，说明**框找得到但贴不准**
- D-FINE 的核心改动正好是提升定位质量，理论上直接命中这个症状

### 4.2 环境与安装

```bash
# 优先试 HuggingFace 路线
pip install -U transformers
python -c "from transformers import DFineForObjectDetection; print('ok')"

# 如果 HF 没有，退回官方仓库
git clone https://github.com/Peterande/D-FINE
cd D-FINE && pip install -r requirements.txt
```

**环境风险**：中等。可能需要处理依赖版本冲突。

### 4.3 数据准备

需要 **COCO JSON 格式**。编写转换脚本：`tiles_train/labels/*.txt`（YOLO 归一化 xywh）→ `annotations/train.json`（COCO 绝对 xywh）。注意 category_id 从 1 开始（COCO 惯例），YOLO 从 0 开始。

### 4.4 训练

改官方 config：`num_classes: 9`、`eval_spatial_size: [1280, 1280]`、`train_dataloader.batch_size: 2`，加载 `dfine_m_coco.pth` 预训练权重。

### 4.5 判读

| 结果 | 判断 |
|---|---|
| mAP50-95 涨幅 > mAP50 | "框不准"诊断成立，定位精修有效 |
| mAP50 和 50-95 同步涨 | 整体提升，但不止是定位的贡献 |
| 都不涨 | 问题不在框回归，回到 assignment / 预训练域 |

**触发条件**：RT-DETR 的 mAP50-95/mAP50 比例仍然 ≤60% 时，跑 D-FINE。

---

## 五、方案 3：Co-DINO（探上限）

### 5.1 为什么

- 一对一匹配的已知缺点是正样本太少、监督信号稀疏，在 3200 张小数据集上更致命
- Co-DETR 在训练时**额外挂一对多辅助头**补监督信号，推理时只留一对一分支
- COCO 上长期 SOTA 之一

### 5.2 定位

**这不是实用方案，是"探上限"用。** 用它回答：堆最强的检测器，这份数据能到多少？如果 Co-DINO 也只有 ~0.22，那 0.2 附近就是数据决定的，停止折腾架构。

### 5.3 环境

依赖 mmdetection，环境配置成本高：

```bash
conda create -n mmdet python=3.11 -y && conda activate mmdet
pip install torch --index-url https://download.pytorch.org/whl/cu124
pip install -U openmim && mim install mmengine "mmcv>=2.0.0" mmdet
```

### 5.4 显存风险

Swin-L + 5scale 在 1280 分辨率上 8GB 大概率 OOM。两个选择：
- 降到 R50 backbone + 800 分辨率（但分辨率下降本身会掉点，成为混淆变量）
- 租一张 24GB 卡跑（最干净的做法）

### 5.5 判读

| 结果 | 判断 |
|---|---|
| > 0.25 | 架构空间还很大，继续挖掘 |
| ~0.22 | 接近数据天花板，回头优化数据 |
| ~0.20 | 确认天花板，转数据增强/合成数据 |

**触发条件**：RT-DETR 和 D-FINE 都无明显提升时，租卡跑一次确认上限。如果前两个方案已经有明显提升，可以跳过。

---

## 六、方案 4：DINOv2 冻结骨干

### 6.1 为什么

- COCO 是彩色自然场景，你的数据是灰度工业成像——**预训练域差距大**，ImageNet/COCO 有监督特征在工业灰度图上迁移性差
- DINOv2 自监督特征公认比有监督特征更通用
- 冻结骨干后只有几 M 可训参数，**长尾过拟合风险大幅下降**（qilie 31 例、huashang 35 例）
- 恰好针对你已确认的 cls_loss 过拟合问题：可训参数减少 → 分类头不再严重过拟合

### 6.2 架构

```
DINOv2 ViT-B/14 (完全冻结)
    ↓ 取多层 patch token，reshape 成 2D 特征图
简单 FPN (可训练)
    ↓
YOLO 检测头 (可训练)
```

### 6.3 环境

```bash
pip install timm
python -c "import timm; m=timm.create_model('vit_base_patch14_dinov2.lvd142m', pretrained=True); print('ok')"
```

### 6.4 关键优化 —— 特征缓存

骨干冻结后同一张图的特征永远不变，可以**预先全部算好存磁盘**，训练时只跑检测头，单 epoch 降一个数量级，轻松跑 30+ epoch。

代价：6000 张 × 4 层特征图可能几十 GB，注意磁盘空间。

### 6.5 判读

| 结果 | 判断 |
|---|---|
| 比 YOLO 高 > 0.05 | 预训练域是关键瓶颈 → 换更大 DINOv3 + 解冻最后几层微调 |
| 稀有类 AP 明显改善 | 长尾过拟合假设成立 |
| 仍在 0.20 附近 | 两套完全不同的特征提取器同分 → 确认数据天花板 |

**触发条件**：RT-DETR 结果出来后就值得并行尝试（环境成本低），尤其是如果 RT-DETR 稀有类 AP 也没有明显改善。

---

## 七、建议执行顺序

```
Day 1：方案 1（RT-DETR 速筛 15 epoch）
  │  ultralytics 内置，零数据改动，一条命令。成本最低、信息量最大。
  │
  ▼
Day 2：按 RT-DETR 结果分叉
  ├── RT-DETR > 0.25  → 上全量 60 epoch，同时并行方案 4（DINOv2）验证是否还能更高
  ├── RT-DETR ~0.20  → 数据天花板嫌疑 ↑
  │   ├── 并行方案 4（DINOv2），若也只有 ~0.20 → 确认天花板，停止架构折腾
  │   └── 并行方案 2（D-FINE），若 mAP50-95 比例仍低
  └── RT-DETR < 0.15 → 先看 loss 曲线是否还在降，决定是否加 epoch
```

```
Day 3–5（仅在 Day 1–2 没有明确突破时）：
  └── 方案 3（Co-DINO）租卡探上限
      如果 Co-DINO 也只有 ~0.22 → 彻底确认：这份数据的天花板就在 0.20～0.22
      → 停止换模型，转数据层面（标注质量、tiling 策略、合成数据、预训练域内微调）
```

速筛阶段用 6000/2000 子集 + 15 epoch 快速排序，选出赢家后再上全量数据重训拿最终成绩。

---

## 八、统一速筛协议

所有候选使用同一套规则，否则数字不可比：

| 项 | 值 |
|---|---|
| 训练集 | `tiles_train` 随机 6000 张（seed=42 固定） |
| 验证集 | `tiles_val` 随机 2000 张（seed=42 固定） |
| Epoch | 15（速筛）→ 赢家全量 60+ |
| Imgsz | 1280 |
| Batch | 2（不够降 1 + 梯度累积） |
| AMP | 开 |
| 评估 | 全部用修复版 `src/utils/metrics.py`，conf=0.001，max_det=300 |
| 指标 | 切片级 mAP50 + mAP50-95 + 整图级（两个都报） |

**速筛绝对值会低于全量训练**，只能用于排序，不能直接和 0.208 比大小。

---

## 九、关键注意事项

1. **评估口径统一**：切片级和整图级是两个不同的数，新实验两个都报
2. **metrics 版本**：确认 0.208/0.206 用的是哪个版本的 metrics.py（修 bug 前后可能不同）
3. **RT-DETR 无 NMS**：方案 1/3 无 NMS 是范式固有差异，如实记录，不硬加
4. **DETR 收敛慢**：15 epoch 只能作为趋势判断，赢家需要 60+ epoch 全量训练
5. **Seed 复验**：架构筛选阶段单 seed 可接受；最终选型至少 2 seed，增益超过 seed 间标准差才算有效
6. **如果所有模型都 ~0.20**：确认数据是天花板，回头优化——标注质量、tiling 策略、数据增强、合成数据
7. **cls_loss 过拟合已经是已知事实**：不再需要两阶段解耦实验来诊断，直接在选择新模型时优先考虑缓解分类过拟合的方案（DINOv2 冻结骨干、D-FINE 的定位-分类解耦设计）
