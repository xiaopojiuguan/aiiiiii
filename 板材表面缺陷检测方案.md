# SteelGuard-YOLO：面向强伪影、长尾与跨产线偏移的板材表面缺陷检测方案

> 赛题：2026AIC·“AI＋钢铁”板材表面缺陷检测  
> 方案定位：**单模型、高召回、可落地、可消融验证**  
> 推荐主模型：**YOLO11m-P2（单阶段 CNN 检测器）**  
> 备选轻量模型：YOLO11s-P2；对照模型：RT-DETR-R50（只做实验对照，不参与最终集成）

---

## 1. 方案摘要

本赛题的核心并不是简单地更换一个更大的检测器，而是同时解决以下四个相互耦合的问题：

1. 原图为典型的 `4096×3000` 超高分辨率灰度图，缺陷可小至二十像素左右，直接缩放会造成信息消失；
2. 水渍、油污、氧化铁皮、纵向亮暗条纹和板材边缘会形成大量“类缺陷”伪影；
3. 9 类缺陷分布极不均衡，气裂等致命缺陷样本极少，但业务上要求优先保证召回率；
4. 后续阶段包含跨产线数据，亮度、纹理、相机位置和板材规格均可能发生分布偏移。

因此提出 **SteelGuard-YOLO**。方案先建立可独立提交的强基线，再让自研模块通过消融实验竞争进入最终模型：

- **强基线**：YOLO11m-P2 + ROI/黑边处理 + 1280 高分辨率切片 + 全局视图 + 类别感知采样 + 基础灰度域增强；
- **候选结构模块**：AAFM 与 MFFM 分别验证独立贡献，再验证二者是否互补；P2/P3 的局部残差辅助分支只在 AAFM 配置启用时使用，避免在约 3200 张图上未经证明地叠加参数；
- **候选长尾策略**：Copy-Paste 由矩形羽化基线逐步升级到局部灰度匹配和残差版本，分类重加权与小目标 NWD 分别消融；
- **候选域泛化策略**：将亮度、对比度、Gamma、噪声等直接灰度增强与 MixStyle 公平比较，不使用测试集伪标签或测试时训练；
- **单模型部署**：最终只有一个模型权重；局部切片和全局图均由同一模型推理。最终配置不是上述模块的固定全集，而是“强基线 + 在 ID、Style-OOD 与复杂度评估中被稳定证明有效的模块”。

---

## 2. 已读取材料与数据认识

### 2.1 赛题任务

模型需要检测以下 9 类缺陷：

| 中文类别 | 提交标签 | 官方给出的样本数量（约） | 特点 |
|---|---:|---:|---|
| 结疤 | `jieba` | 604 | 数量较多，形态变化大 |
| 纵裂 | `zonglie` | 296 | 细长、可能跨越较大范围，致命缺陷 |
| 气裂 | `qilie` | 28 | 极端长尾，优先保证召回 |
| 夹杂 | `jiaza` | 113 | 可非常细小，也可能呈细长形态 |
| 异物压入 | `yiwuyaru` | 464 | 合并了多种原始缺陷，类内差异大 |
| 划伤 | `huashang` | 60 | 稀有、细长，容易和水切痕混淆 |
| 麻面麻坑 | `mamianmakeng` | 309 | 点状或局部密集纹理 |
| 氧化铁皮 | `yanghuatiepi` | 358 | 小块状、密集或离散分布 |
| 辊印 | `gunyin` | 236 | 局部纹理异常，可能与正常轧制纹理相似 |

官方说明约有 3200 张训练图和约 800 张初赛测试图。本地目录的文件元数据表明，当前版本约为 **3215 组训练图像/XML 与 660 张测试图像**；最终仍应以实际提交阶段下发的数据为准。

### 2.2 标注格式与工程注意事项

- 图像主要为 `4096×3000`、8-bit 灰度工业图；
- 标签为 PASCAL VOC XML，边界框为绝对坐标 `[xmin, ymin, xmax, ymax]`；
- XML 中的 `depth` 同时出现 `1` 和 `3`，但图像视觉上主要为灰度；
- 部分 XML 的 `filename` 字段可能写成 `.xml`，`folder/floder` 字段也不完全规范，因此**不能依赖 XML 内路径字段**，应始终按文件 stem 配对 JPG 和 XML；
- 抽样中存在无 `<object>` 的合法负样本，应保留作为伪影硬负样本，不能全部删除；
- 抽样还显示可能存在空文件或格式不一致迹象，正式训练前应只扫描 XML 文本进行轻量健检，不需要逐张解码全部图像。

### 2.3 小样本抽查得到的尺度证据

抽查的标注中可见：

- `yanghuatiepi` 框约为 `21×19` 像素；
- `jieba` 框约为 `31×63` 像素；
- `jiaza` 也可能达到约 `103×503`，且贴近图像边界；
- 原图直接缩放到 640 时，一个 `21×19` 目标只剩约 `3×4` 像素，几乎不可检测；
- 即使缩放到 1280，最小目标也只有约 `6×8` 像素，因此必须采用原分辨率局部切片和 stride=4 的 P2 检测头。

### 2.4 少量图像视觉观察

从少量训练/测试样例可以观察到：

1. 板面包含明显的纵向亮暗条带、云状水油渍、颗粒纹理及局部反光；
2. 部分图像存在大面积纯黑非板材区域，且板材上下/左右边界位置变化明显；
3. 缺陷与背景对比度低，很多目标只有局部纹理变化，而不是明显的颜色变化；
4. 同一张图中同时存在低频污染和高频颗粒，单纯 CLAHE 会同时放大缺陷与噪声；
5. 测试样例之间的曝光、板面粗糙度和有效 ROI 比例差异较大，说明固定全图归一化不够稳健；
6. 官方示意图中的麻面麻坑、辊印和氧化铁皮均较小，且只有局部放大后才容易识别。

这些观察直接支持“ROI 过滤 + 高分辨率切片 + 抗伪影特征 + 域泛化”的技术路线。

---

## 3. 模型类型选择

## 3.1 推荐：YOLO11m-P2 单阶段检测器

选择 YOLO 系列作为主模型，原因如下：

- 对约 3200 张规模的数据集更容易稳定微调；
- P2 小目标头、切片训练和自定义损失易于实现；
- 推理速度明显优于重型 DETR，适合一张大图需要多切片推理的场景；
- 生态成熟，便于 VOC 转换、AMP 训练、导出 ONNX/TensorRT 和复现；
- 最终仅保存一个权重，不需要多模型投票。

推荐规模为 `m`：

- `s` 速度快，但面对类内差异大和伪影复杂的任务容量可能不足；
- `l/x` 在每张原图需要约 10～15 次切片推理时成本过高，且小数据更容易过拟合；
- `m` 是精度、显存和速度之间较稳妥的平衡。

如果现有代码库只稳定支持 YOLOv8，则可使用 **YOLOv8m-P2** 等价落地；方案的关键不依赖具体版本号。

## 3.2 为什么不把 DETR 作为主模型

RT-DETR/DINO 等 Transformer 检测器具有全局建模优势，但本题存在：

- 极小目标数量较多；
- 训练数据规模偏小；
- 超大图切片后推理次数高；
- 长尾类别需要大量针对性采样。

因此 DETR 更适合作为**独立对照实验**，用于判断 Transformer 全局建模在本数据上的真实收益，而不是默认的最终集成成员。可保留 RT-DETR-R50 对照，但只有在相同数据划分、输入预算和训练计算量下进行比较才有解释力。

---

## 4. SteelGuard-YOLO 总体结构

```text
4096×3000 原始灰度图
        │
        ├── 板材 ROI 检测与黑边掩膜
        │
        ├── 全局视图（长边缩放至 1536） ───────────────┐
        │                                               │
        └── 1280×1280 重叠切片（overlap=256） ──────────┤
                                                        ▼
                      同一个 SteelGuard-YOLO11m-P2 权重
                                                        │
        ┌───────────────────────────────────────────────┤
        │ 原始灰度主干                                  │
        │ 局部对比度/高频残差辅助分支（AAFM 候选）       │
        │ P2/P3 抗伪影门控融合 AAFM（按消融选择）        │
        │ P2-P5 多尺度检测 + MFFM（按消融选择）          │
        └───────────────────────────────────────────────┤
                                                        ▼
            坐标还原 → 类别感知 Soft-NMS/去重 → JSON
```

这里的“全局视图 + 局部切片”不是多模型集成：两种视图共享完全相同的网络和权重，只是输入尺度不同。

---

## 5. 核心创新设计

## 5.1 创新一：ROI 感知的全局—局部双尺度检测

### 5.1.1 板材 ROI 掩膜

少量样例中存在大面积黑边、板材边缘和非检测区域。若直接均匀切图：

- 会浪费大量推理时间；
- 黑边与高亮板边可能产生误检；
- 全图均值/方差会被黑边严重影响。

处理方法：

1. 对原图下采样到小尺寸；
2. 根据亮度、梯度和最大连通区域估计板材主体；
3. 形态学闭运算补全水渍造成的孔洞；
4. ROI 向外扩张 32～64 像素，保留贴边缺陷；
5. 训练和推理时忽略有效板材占比过低的切片。

该步骤是确定性预处理，不修改测试预测，符合规则。

### 5.1.2 局部高分辨率切片

- 切片尺寸：`tile_size = 1280`；
- 重叠：`overlap = 256`，严格使用 `stride = tile_size - overlap = 1024`；
- 对完整 `4096×3000` 图像，在不做 ROI 跳过时通常形成横向 4 × 纵向 3，即约 12 个局部切片；加上 1 次全局视图，单张原图约需 13 次前向；
- 边缘不足处采用 reflect padding，而不是纯黑 padding；
- 正样本训练裁剪必须保证目标中心落在裁剪区域内；
- 对被切断目标：若保留面积小于原框 40%，训练时忽略；否则裁剪框并保留；
- 推理时将局部框映射回原始绝对坐标。

这样 21×19 像素的小缺陷不会因全图缩放而丢失。

### 5.1.3 全局视图

仅使用切片会损失贯穿大范围的纵裂、划伤及整体板面上下文。因此同一个模型额外处理一次全局缩略图：

- 原图保持比例，长边缩放至 1536；
- 全局图主要负责长条缺陷、跨切片目标和上下文判断；
- 局部图主要负责微小目标；
- 最后只对来自重叠区域的同类高 IoU 框进行去重。

该设计比“只切图”更完整，也比“两个模型集成”更轻量。

---

## 5.2 创新二：形态感知抗伪影融合模块

建议将网络创新集中在 AAFM 与 MFFM 两个有明确任务动机的候选模块中，但**不预设二者必须同时进入最终模型**。在约 3200 张图上，组合可能出现参数增量导致的过拟合、功能冗余或特征分布变化带来的优化冲突；也可能分别改善伪影抑制与形态建模而形成互补。因此必须按 `Baseline → AAFM-only → MFFM-only → AAFM+MFFM` 验证，组合仅在收益稳定且计算预算允许时晋级。

### 5.2.1 AAFM：Artifact-Aware Feature Modulation

输入构造两个互补视图：

- 主视图：原始灰度图复制为 3 通道，以最大程度复用预训练权重；
- 辅助视图：局部对比度残差
  
  `R = I - GaussianBlur(I, large_kernel)`

辅助视图弱化大范围亮度、水渍和油污造成的低频变化，突出微裂纹、点蚀和局部边缘。不能直接用 CLAHE 替换原图，因为 CLAHE 也会放大颗粒噪声。

在 P2、P3 层进行门控融合：

```text
F_raw = Backbone(I)
F_res = ShallowConv(R)
Gate  = sigmoid(Conv([F_raw, F_res]))
F_out = F_raw + Gate × F_res
```

门控由原始语义和局部残差共同决定，使网络自行选择何时增强细节、何时抑制伪影。

### 5.2.2 MFFM：Multi-Morphology Feature Fusion

板材缺陷主要存在三种形态：

- 点状/小块：夹杂、麻坑、氧化铁皮；
- 纵向细长：纵裂；
- 横向或不规则长条：划伤、部分气裂。

在 Neck 的 P2/P3 融合节点使用并行轻量卷积：

- `3×3`：局部块状纹理；
- `1×7`：横向细长结构；
- `7×1`：纵向细长结构；
- 可变形卷积 DCNv2：不规则轮廓。

四路特征通过可学习权重归一化融合。模块只放在高分辨率层，避免显著增加整网计算量。

### 5.2.3 为什么该创新适合本题

- 不是泛化地“加一个注意力模块”，而是由板材图像的低频污染和方向性缺陷直接推导；
- 原始强度分支保留真实材质，残差分支突出细小缺陷；
- 非对称卷积显式适配裂纹与划伤方向；
- AAFM 可用伪影负样本误检率和小目标召回验证，MFFM 可用点状/纵向/横向目标召回验证。

### 5.2.4 模块准入与复杂度约束

四组结构实验必须使用相同的数据划分、训练预算和基础增强，并共同报告：

- ID 验证集与 Style-OOD 验证集的 mAP、Macro Recall；
- 每类 AP，尤其是小目标、纵裂、划伤与气裂；
- 训练/验证曲线差距和至少 2～3 个关键随机种子的均值、标准差；
- 参数量、单 tile FLOPs、峰值显存以及单张原图端到端时延；
- AAFM 与 MFFM 分别插入和组合后的增量，而不是只报告最终模型总量。

若双模块组合只提高训练或 ID 指标，却降低 Style-OOD，或者增益小于随机种子波动，则只保留贡献更稳定的单模块。若两者提升的类别和错误类型不同，则再通过组合实验确认互补性，并为组合重新搜索学习率和权重衰减，避免把优化参数不匹配误判为模块无效。

---

## 5.3 创新三：面向致命缺陷的长尾高召回学习

### 5.3.1 类别感知采样

不建议简单按 9 类完全均衡，否则 28 个气裂样本会被重复到严重过拟合。采用温度平滑后的重复因子：

```text
p(c) ∝ n(c)^(-α), α 建议从 0.4～0.6 搜索
```

并限制每个原始稀有样本每轮最多重复 3～4 次。

训练裁剪组成建议：

- 60%：以真实缺陷为中心的正样本裁剪；
- 20%：稀有类优先裁剪；
- 15%：无缺陷或上一阶段误检区域的硬负样本；
- 5%：ROI 内随机裁剪。

### 5.3.2 稀有缺陷 Copy-Paste 递进实验

残差 Copy-Paste 的难点在于估计“缺陷下方原本的板面”。结疤、夹杂等缺陷与背景纹理混合较深，直接执行“目标块 − 平滑背景”可能把正常纹理一起提取出来，产生边缘光晕或频率伪影。因此不把复杂残差版本作为主线依赖，而采用由简到繁的实验链：

1. **CP-0 矩形基线**：复制目标框，限制粘贴到亮度和纹理相近的 ROI；
2. **CP-1 边缘羽化**：使用软边或距离变换消除矩形硬边；
3. **CP-2 局部灰度匹配**：匹配源块与目标背景的均值、方差、亮度和对比度；
4. **CP-3 前景掩膜版本**：若能稳定得到粗缺陷掩膜，只迁移缺陷区域；
5. **CP-4 残差版本**：估计局部背景后提取缺陷残差，再以软掩膜叠加到新背景。

各版本优先用于 `qilie`、`huashang`、`jiaza`、`zonglie` 等稀有类，每张图最多粘贴 1～3 个。只有前一级在**不含合成样本的真实 ID/Style-OOD 验证集**上有效，才继续投入下一级；复杂版本必须显著优于 CP-1/CP-2 才进入最终训练。还需保存可视化样本并检查模型是否学习粘贴边缘、光晕或背景泄漏，而不能只看含合成数据的训练指标。

### 5.3.3 分类与回归损失

分类建议使用带类别有效样本权重的 Quality Focal Loss：

```text
w_c = (1 - β) / (1 - β ^ n_c)
```

权重需截断，例如限制在 `[0.5, 3.0]`，防止极稀有类梯度失控。

边界框使用尺度自适应混合损失：

```text
L_box = L_CIoU + λ_small × L_NWD
```

- NWD（Normalized Wasserstein Distance）对几像素级目标的位置偏移更稳定；
- `λ_small` 随目标面积减小而增大，大目标接近 0；
- 对长宽比很大的框保留方向/形状约束，减少长裂纹定位偏差。

### 5.3.4 自定义本地计分与阈值策略

公告没有给出 IoU 阈值、Precision/Recall 加权比例，也没有说明按完整 PR 曲线还是固定置信度点评分。因此本方案不再等待外部规则补充，而是在任何模型比较前冻结一套本地评估协议，并对所有实验统一执行：

- **AP 主指标**：使用 COCO 风格的 `mAP@0.5:0.95`，IoU 从 `0.50` 到 `0.95`、步长 `0.05`；同时报告 `mAP@0.5`，前者衡量综合定位质量，后者衡量工业缺陷是否被基本检出；
- **P/R 主工作点**：固定 `IoU=0.50`，同类别预测按置信度降序与真实框做一对一贪心匹配，每个真实框最多匹配一次；额外报告 `IoU=0.75` 作为定位严格度敏感性分析；
- **普通类别 P/R 权重**：使用 `F1 = 2PR/(P+R)`，Precision 与 Recall 等权；
- **致命类别 P/R 权重**：`qilie`、`zonglie` 使用 `F2 = 5PR/(4P+R)`，Recall 权重为 Precision 的 4 倍，体现漏检代价更高；
- **宏平均方式**：Macro F1 为 9 类 F1 的算术平均；Critical Macro F2 为 `qilie` 与 `zonglie` 两类 F2 的算术平均；没有标注实例的类别不参与该图像级计算，但在全验证集类别统计中保留为 0/不可用并单独记录；
- **阈值选择**：在固定主验证集按 `0.01` 步长扫描 `[0.05, 0.95]`；普通类别选择使 F1 最大的阈值，致命类别选择使 F2 最大的阈值，并同时报告 FP/image，防止以不可接受的误报换取召回；
- **低分候选与数量上限**：完整 PR/AP 使用统一预过滤阈值 `0.001`；每图后处理最多保留全局 `max_det=300` 个候选，若候选接近上限则额外报告 `max_det=100/500` 的敏感性；固定阈值 P/R 和最终 JSON 使用验证集确定的 9 类阈值；
- **数据纪律**：阈值只能由固定主验证集确定，再到其他 seed、折和 Style-OOD 上复验，禁止根据测试榜单逐类调参。

本地选模同时保留完整曲线指标和固定阈值指标，从而避免结果只适用于一种未知的官方评分实现。上述 IoU、F1/F2 和综合分权重是团队的**内部评估协议 v1**，不是对官方公式的推断；协议冻结后不得因某个候选模型的结果而临时改权重。为检查结论是否依赖单一 IoU，可额外报告 `IoU=0.75` 下的 P/R，但只用 `IoU=0.5` 的 F1/F2 作为主阈值选择依据。类别阈值属于后处理优化，不作为核心结构创新。

---

## 5.4 创新四：不接触测试标签的跨产线域泛化

赛题明确禁止将测试集或测试伪标签混入训练，因此本方案只在训练集内部构造域变化。

### 5.4.1 训练集风格随机化

- 灰度 Gamma：模拟曝光差异；
- 局部对比度和动态范围压缩：模拟不同光源；
- 低频亮度场：模拟水渍、油污和不均匀照明；
- 纵向/横向弱条纹：模拟线阵相机和轧制纹理；
- 轻度高斯、泊松噪声和运动模糊；
- 低频 Fourier 幅度扰动：改变风格但尽量保留高频缺陷结构。

增强必须有上限，并对目标区域做可见性检查，不能把真实缺陷完全抹掉后仍保留标签。

### 5.4.2 MixStyle 与直接灰度域增强对照

MixStyle 在主干浅层 C2/C3 混合中间特征的通道均值和方差，只在训练阶段启用，推理阶段无额外计算。虽然输入灰度图复制后的三个通道相同，但经过不同卷积核后，中间特征通道并不会保持相同，因此不能据此直接断言 MixStyle 无效。

不过，本题的域偏移主要可能来自曝光、对比度、噪声、模糊与板面纹理，简单的灰度域增强可能已经覆盖主要变化。应公平比较：

```text
Baseline
Baseline + Brightness/Contrast/Gamma/Noise
Baseline + MixStyle
Baseline + 灰度域增强 + MixStyle
```

MixStyle 概率以 0.1、0.3 为起点搜索，并观察其是否稳定提升 Style-OOD，而不是只提高 ID 指标。若收益没有超过简单灰度增强或落在 seed 波动内，则不进入最终模型。

### 5.4.3 域压力验证集

从训练数据内部构建两个验证维度：

1. **Group-Stratified 验证**：按文件名前部的板材/批次标识分组，避免相邻帧同时落入训练和验证导致泄漏；
2. **Style-OOD 验证**：根据亮度、对比度、纹理强度和 `RawXX` 相机位构造风格差异较大的验证子集。

只有同时提升常规验证和 Style-OOD 验证的模块，才进入最终模型。

---

## 6. 数据处理方案

## 6.1 轻量数据健检

只解析 XML，不必读取全部大图像像素：

- 检查 JPG/XML 是否按 stem 一一对应；
- 检查 XML 是否可解析、是否为空文件；
- 检查类别是否严格属于 9 类；
- 检查 `xmin < xmax`、`ymin < ymax`；
- 检查坐标是否越界；
- 检查完全重复框；
- 统计空标注图，保留为负样本；
- 从 XML 的 size 字段统计尺寸和目标尺度。

只有发现异常时才针对性读取对应图片，不需要遍历解码全部数据。

## 6.2 灰度输入

建议不直接把网络第一层改成 1 通道，避免破坏 COCO 预训练权重：

- 原灰度复制为 RGB 三通道进入主干；
- 局部残差通过独立轻量辅助分支进入 AAFM；
- 归一化只基于板材 ROI，不包含大面积黑边；
- 训练与测试使用完全相同的预处理。

## 6.3 合理增强与不建议增强

推荐：

- 水平翻转；
- 轻微缩放、平移；
- 灰度 Gamma/对比度变化；
- 轻度模糊和噪声；
- 受控的低频污渍模拟；
- 通过递进实验筛选出的稀有类 Copy-Paste；
- Mosaic 仅在前期低概率使用，并在训练最后 30% 的计划 epoch/迭代中关闭，使最终精调重新贴近真实图像分布。

谨慎或不建议：

- 90° 随机旋转：会破坏产线方向先验；
- 强透视变换：工业相机通常固定，过强变换不真实；
- 过强 CLAHE：会同步放大颗粒伪影；
- 大比例 MixUp：会降低微裂纹对比度；
- 无限制复制稀有类：会导致背景泄漏和过拟合。

---

## 7. 训练流程

## 7.1 数据划分

建议使用 5 折 Group-Stratified 划分，但只训练一个最终模型：

- 分组键优先使用文件名中 `RawXX` 前的板材/批次编号；
- 保证同批次相邻帧不跨训练/验证；
- 每折尽量保留 9 类，尤其是 28 个气裂样本；
- 开发阶段选择一折作为固定主验证，避免每次实验反复更换验证集；
- 关键结构和增强结论至少使用 2～3 个随机种子复验，重要候选再用其余折确认稳定性；
- 最终确定超参数后，使用全部训练数据重新训练**一个权重**。

多折与多 seed 只用于实验可靠性，不默认在最终推理中做权重集成。所有划分文件、随机种子和配置哈希从第一天起保存，防止实验不可复现。

## 7.2 分阶段训练

### 阶段 A：建立可信强基线

- YOLO11m，COCO 预训练；
- `1280×1280` 局部切片、ROI 处理、全局视图和 P2 检测头；
- 类别感知采样与基础亮度/对比度/Gamma 增强，但不加 AAFM、MFFM、MixStyle 和复杂 Copy-Paste；
- 训练 80～120 epoch 作为起点，以固定验证集早停，得到 baseline、误检清单和端到端测速结果。

### 阶段 B：候选模块独立消融

- 分别训练 AAFM-only、MFFM-only、NWD、Copy-Paste 递进版本和 MixStyle/灰度增强对照；
- 每次只改变一个主要因素，保留相同划分、训练预算和评估脚本；
- 阶段 A/B 的 epoch 只是起始范围，按验证曲线早停，不机械把各阶段相加为固定总 epoch；
- 对进入候选名单的配置再用 2～3 个 seed 复验。

### 阶段 C：组合与低增强精调

- 仅组合阶段 B 中独立有效且错误类型互补的模块，重点验证 AAFM+MFFM 是否超过单模块；
- 关闭 Mosaic、强污渍模拟和强 Copy-Paste，保留通过验证的轻度亮度/噪声增强；
- 降低学习率 5～10 倍，推荐 20～40 epoch 作为精调起点；
- 以 ID mAP、Style-OOD mAP、Macro Recall、稀有/致命类召回、FP/image 和推理成本共同选择 checkpoint；
- 训练预算用 optimizer steps 与 GPU-hours 记录，避免只用 epoch 比较不同数据配置。

## 7.3 推荐超参数起点

| 参数 | 建议起点 |
|---|---|
| Optimizer | AdamW |
| 初始学习率 | `2e-4`（按总 batch 线性调整） |
| Weight decay | `5e-4` |
| Warmup | 3～5 epoch |
| Scheduler | Cosine decay |
| Tile size | `1280×1280` |
| Tile overlap | `256` |
| Batch size | 16GB 显存约 2～4，使用梯度累积到等效 16 |
| Precision | AMP FP16/BF16；测速时单独记录 FP32/FP16 |
| EMA | 开启 |
| Early stopping | 不只看总 mAP，需监控稀有类召回与 Style-OOD |
| 随机种子 | 关键创新至少 2～3 个种子复验，最终固定一个 |
| 训练预算 | 记录 optimizer steps、实际 epoch、GPU-hours，不机械限定总 epoch |
| 复杂度记录 | Params、单 tile FLOPs、峰值显存、预处理/前向/后处理时延 |

---

## 8. 推理与后处理

1. 读取原图并估计板材 ROI；
2. 生成一张全局缩略图；
3. 在 ROI 上生成 `1280×1280`、重叠 256、`stride=1024` 的局部切片；
4. 优先将多个局部切片组成 batch 推理，另对全局视图推理一次；显存不足时再降到较小 batch；
5. 将所有框映射回 `4096×3000` 原始坐标；
6. 裁剪越界坐标，删除宽高非正框；
7. 类别内进行 Soft-NMS 或高 IoU 去重：
   - 普通类别可从 IoU 0.5～0.6 搜索；
   - 细长裂纹需谨慎，不能把相邻但不同的裂纹错误合并；
8. 完整 PR/AP 评估使用统一低预过滤阈值 `0.001`；固定工作点评估与最终 JSON 使用主验证集分别按普通类 F1、致命类 F2 选出的类别阈值，并记录过滤前后候选数量；
9. 输出官方 JSON。

### 8.1 端到端测速与加速顺序

不能只报告模型 forward 时间。每个候选模型都记录固定硬件、输入尺寸、batch size、FP32/FP16、预热次数和随机种子，并拆分统计：图像读取、ROI/切片生成、CPU-GPU 传输、模型前向、坐标映射、NMS/WBF 和 JSON 序列化。

至少报告单图平均/P95 时延、平均局部切片数、峰值显存、吞吐量和最坏样本时延。若官方有推理上限，优先采用以下顺序优化：

1. 跳过高比例黑边和无效 ROI；
2. 多切片 batch inference；
3. FP16/AMP；
4. 优化预处理、数据传输与后处理；
5. TensorRT FP16（比赛环境允许时）；
6. 最后才评估局部切片使用 s 模型或降低全局输入尺寸。

若官方不计推理时间，只需确认总提交窗口内能完成测试集推理，不应为了理论速度提前牺牲已验证的精度。

输出示例：

```json
[
  {
    "image_id": "0001400166-Raw00-f_00006.jpg",
    "category_name": "gunyin",
    "bbox": [2560, 1520, 2615, 1539],
    "score": 0.95
  }
]
```

提交前自动校验：

- `image_id` 必须来自测试集原文件名；
- `category_name` 必须严格为官方 9 类拼音；
- `bbox` 必须为 `[xmin, ymin, xmax, ymax]` 绝对坐标；
- `0 ≤ xmin < xmax ≤ width`；
- `0 ≤ ymin < ymax ≤ height`；
- `score` 为 `[0,1]` 浮点数；
- JSON 使用英文双引号，不使用网页示例里的中文引号。

---

## 9. 评估指标与选模原则

赛题页面同时提到 Precision、Recall 和 mAP，但没有给出完整公式、IoU 阈值或固定阈值/完整 PR 曲线的实现方式。为保证 AI 可以直接执行，项目启动时必须生成并冻结版本化的本地评估配置；除非公告后续正式补充规则，否则所有实验均按本方案的统一协议比较，并至少报告：

- `mAP@0.5`；
- `mAP@0.5:0.95`；
- Macro Precision / Macro Recall；
- 每类 AP、Precision、Recall；
- 小目标 AP/Recall；
- 细长目标 Recall；
- 致命类召回：`qilie`、`zonglie`；
- 每张图误报数 FP/image；
- `IoU=0.5` 下的 Macro F1，以及 `qilie/zonglie` 的 Macro F2；
- 普通类 F1 最优阈值、致命类 F2 最优阈值及对应 FP/image；
- ID 与 Style-OOD 的差值及各类别变化；
- 单 tile Params/FLOPs、单张原图平均切片数、预处理/前向/后处理耗时、平均/P95/最坏时延、峰值显存。

建议内部选模分数作为统一排序工具，但不作为唯一决策。各项先归一化到 `[0,1]`：

```text
Score_internal = 0.35 × mAP@0.5:0.95
               + 0.20 × mAP@0.5
               + 0.15 × MacroF1@IoU0.5
               + 0.15 × CriticalMacroF2@IoU0.5
               + 0.15 × StyleOOD_mAP@0.5:0.95
```

其中 `CriticalMacroF2` 为 `qilie` 与 `zonglie` 两类 F2 的宏平均。为避免内部综合分掩盖严重退化，任何候选若使 Style-OOD 主指标或任一致命类召回出现超过 seed 波动的稳定下降，即使总分上升也不得直接晋级。

只有满足以下条件，候选模块才进入最终模型：

1. 在固定真实验证集和 Style-OOD 验证集上方向一致，且关键提升不只是单个 seed 的偶然结果；
2. 关键创新至少 2～3 个 seed 复验，增益明显超过标准差或至少没有稳定退化；
3. 对目标错误类型确有解释力，例如抗伪影模块降低 FP/image、MFFM 提升形态类别召回；
4. 额外 Params/FLOPs/显存/端到端时延符合官方约束或团队提交预算；
5. 没有因复杂增强产生明显合成伪影、数据泄漏或规则合规问题。

该分数只用于团队内部选型，最终仍以官方指标为准。

---

## 10. 分层消融实验设计

创新性必须由实验支撑，不能只描述模块。时间充足时不需要预先砍掉候选方向，但应采用分层筛选，避免把所有因素一次性全排列。除 B0 外，每项增量实验都使用相同的 Group-Stratified 主验证集和训练预算；关键晋级配置再用 2～3 个随机种子复验。

### 10.1 系统基础与强基线

| 实验 | 改动 | 重点观察 | 要回答的问题 |
|---|---|---|---|
| B0 | YOLO11m，整图缩放 1280 | 总体 mAP、小目标召回 | 直接缩放损失多大？ |
| B1 | B0 + 1280 切片 | 小目标 AP、端到端时延 | 高分辨率切片是否必要？ |
| B2 | B1 + P2 head | `yanghuatiepi/jiaza` 召回 | stride=4 是否有效？ |
| B3 | B2 + ROI 与全局视图 | 纵裂/划伤召回、FP/image | ROI 和全局上下文能否同时改善？ |
| B4 | B3 + 类别感知采样/基础灰度增强 | 长尾召回、Style-OOD | 这是否构成可独立提交的强基线？ |

### 10.2 AAFM/MFFM 结构模块

| 实验 | 改动 | 重点观察 | 要回答的问题 |
|---|---|---|---|
| S0 | B4（强基线） | 全部指标 | 结构模块的共同基准是什么？ |
| S1 | B4 + AAFM（AAFM-only） | 伪影负样本 Precision、FP/image、小目标召回 | AAFM 是否独立有效？ |
| S2 | B4 + MFFM（MFFM-only） | 点状/纵向/横向目标 AP | MFFM 是否独立有效？ |
| S3 | B4 + AAFM + MFFM | ID、Style-OOD、复杂度和 seed 波动 | 两者是否互补，还是冗余/冲突？ |

### 10.3 长尾、域泛化与后处理

| 实验 | 改动 | 重点观察 | 要回答的问题 |
|---|---|---|---|
| T1 | B4 + QFL/类别重加权 | 气裂、划伤召回 | 分类重加权是否有稳定收益？ |
| T2 | B4 + NWD | 小目标定位 AP/Recall | 小框回归是否更稳定？ |
| T3 | B4 + CP-0/CP-1/CP-2 | 真实验证集与合成伪影检查 | 简单 Copy-Paste 是否已经有效？ |
| T4 | 仅当 T3 有效时加入 CP-3/CP-4 残差版本 | OOD、可视化自然度 | 残差版本是否值得复杂度？ |
| T5 | B4 + 直接灰度增强 | Style-OOD | 简单域随机化能覆盖多少偏移？ |
| T6 | B4 + MixStyle | Style-OOD | MixStyle 是否超过直接灰度增强？ |
| T7 | B4 + 灰度增强 + MixStyle | Style-OOD、ID 差值 | 二者是否互补？ |
| T8 | B4 + hard negative mining | Precision、FP/image | 是否真正抑制水渍/条纹误报？ |
| T9 | NMS/Soft-NMS/WBF、F1/F2 类别阈值 | 本地冻结协议、FP/image 与候选数量 | 哪种后处理在完整 AP 和固定工作点上都更稳？ |

所有表格填写真实结果，不预先虚构提升幅度。每项创新至少报告：ID 与 Style-OOD 均值/标准差、每类 AP/Recall、额外参数量、单 tile FLOPs、峰值显存、平均/P95 端到端时延和 GPU-hours。最终只保留达到准入条件的模块，不因技术报告需要“更多创新点”而强行叠加。

---

## 11. 优先级与实施路线

时间充足不等于所有模块都应无条件进入最终模型。优先级表示实验和交付顺序，而不是对候选模块有效性的预判。

## 11.1 P0：强基线、评估协议与可提交闭环

1. VOC 标签健检与转换；
2. 按批次分组划分训练/验证，并构建 Style-OOD 子集；
3. ROI + `1280` 重叠切片，明确 `stride=1024`；
4. YOLO11m-P2 强基线；
5. 全局/局部同权重推理；
6. 坐标还原、NMS 和 JSON 校验；
7. 冻结本地评估协议：`mAP@0.5:0.95`、`mAP@0.5`、普通类 F1、致命类 F2、Style-OOD 和 FP/image；
8. 固定候选匹配、低分预过滤、`max_det` 和类别阈值搜索实现，并将配置版本化；
9. 建立端到端测速脚本和实验日志。

完成后应形成可独立提交、可复现、可测速的强基线，不依赖 AAFM、MFFM、MixStyle 或残差 Copy-Paste。

## 11.2 P1：核心模块与误差类型消融

1. AAFM-only、MFFM-only、AAFM+MFFM；
2. 类别感知采样、QFL/类别重加权和小目标 NWD；
3. Copy-Paste 的 CP-0～CP-4 递进验证；
4. 直接灰度增强与 MixStyle 对照；
5. hard negative mining；
6. 对进入候选的配置做 2～3 seed 和 Style-OOD 复验。

## 11.3 P2：扩展实验、后处理与部署优化

1. RT-DETR-R50 独立对照；
2. NMS、Soft-NMS、WBF，以及普通类 F1/致命类 F2 驱动的类别阈值搜索；
3. TTA 或有限的结构差异实验；
4. 切片 batch inference、FP16、TensorRT/ONNX 加速；
5. 只在最终模型满足精度准入后进行部署固化。

最终模型的形式是“强基线 + 被 ID/Style-OOD、关键类别、稳定性和复杂度共同证明有效的候选模块”，不是 P1/P2 项目的简单全集。

---

## 12. 分阶段实验计划

以下按实验依赖关系组织，不规定每个阶段必须在某个日期完成，也不把模块数量或训练 epoch 作为硬性进度承诺。AI 可以在完成前置条件后自动进入下一阶段；如果某个候选模块没有稳定收益，应停止该分支，而不是为了赶时间继续堆叠。

### 全程实验管理（每个阶段都执行）

- **环境固化**：记录 Python、PyTorch、CUDA、GPU、驱动、依赖版本、代码提交号和数据版本；任何关键实验都保存可复现运行命令；
- **日志记录**：统一保存配置文件、训练日志、验证结果、checkpoint、显存、GPU-hours、错误样本和可视化结果；
- **随机性控制**：固定并记录数据划分、随机种子、采样器状态、数据增强配置和 early stopping 状态；关键结论至少使用 2～3 个 seed 复验；
- **评估记录**：每个实验同时记录 ID、Style-OOD、逐类 AP/Recall、FP/image、单 tile 复杂度和单图端到端时延；
- **报告同步**：每完成一个实验组就更新消融表、风险记录和技术报告草稿，不把环境整理、证据整理和报告撰写推迟到最终阶段；
- **可回退性**：保留强基线 checkpoint 和所有晋级/淘汰依据，任何候选模块都不能破坏可独立提交的强基线。

### 阶段 1：数据、规则与闭环

- 完成 XML 健检、类别映射、Group Split 和数据版本清单；
- 冻结并版本化本地评估协议、IoU 匹配方式、低分预过滤、`max_det`、F1/F2 阈值搜索和提交格式；
- 完成 ROI 与切片数据加载器；
- 跑通 YOLO11m/YOLO11m-P2 的训练、推理、坐标还原和 JSON 校验；
- 建立固定验证脚本、端到端测速脚本、日志目录和配置记录。

### 阶段 2：强基线与误差分析

- 固定 `1280` 切片、`overlap=256`、`stride=1024`、全局视图和 P2；
- 完成 ROI/黑边处理、类别感知采样和基础灰度域增强；
- 输出强基线的 ID/Style-OOD 指标、逐类错误、黑边/水渍/条纹误检清单；
- 记录平均切片数、平均/P95 时延、显存和 GPU-hours。

### 阶段 3：小目标、长尾与 Copy-Paste

- 独立验证 QFL/类别重加权和 NWD；
- 按 CP-0 → CP-1 → CP-2 → CP-3/CP-4 递进验证 Copy-Paste；
- 检查合成样本自然度和真实验证集是否提升；
- 对已显示稳定收益的配置进行首轮多 seed 复验。

### 阶段 4：AAFM/MFFM 独立消融

- 完成 AAFM-only、MFFM-only 与强基线对照；
- 分析伪影误检、小目标、点状和细长目标的变化；
- 测量 Params、FLOPs、显存和端到端时延增量；
- 对独立有效的模块重新调学习率/权重衰减，避免优化参数不匹配。

### 阶段 5：组合、域泛化与对照模型

- 验证 AAFM+MFFM 是否超过单模块并检查 Style-OOD；
- 比较直接灰度增强、MixStyle 及二者组合；
- 完成 hard negative mining；
- 在相同数据与预算下完成 RT-DETR-R50 独立对照；
- 汇总第一版模块晋级表。

### 阶段 6：后处理、加速与多 seed 复验

- 按冻结的本地协议搜索 NMS/Soft-NMS/WBF，并分别用普通类 F1、致命类 F2 确定类别阈值；
- 对 2～3 个最优候选完成多 seed、其他分组或交叉折复验；
- 进行切片 batch inference、FP16、TensorRT/ONNX（环境允许）测速；
- 形成最终模型与强基线的精度—复杂度对比表。

### 阶段 7：最终固化与提交

- 使用全部训练数据重新训练一份最终权重，不默认做多模型集成；
- 固定随机种子、环境、依赖版本、配置、推理命令和输出校验；
- 完成盲测/伪盲测、最坏时延和提交文件审计；
- 完成技术报告，确保每个保留模块都有真实消融证据，未验证或无稳定收益的模块不强行写入最终配置。

---

## 13. 风险与控制措施

| 风险 | 表现 | 控制措施 |
|---|---|---|
| 稀有类过采样过度 | 训练召回高、验证召回低 | 限制重复次数，使用分组验证和轻量 Copy-Paste |
| AAFM/MFFM 组合过拟合或冗余 | 单模块有效，组合在 Style-OOD 下降或增益小于 seed 波动 | 四组独立/组合消融，监控训练验证差距、参数增量和错误类型互补性 |
| 强增强破坏缺陷 | 标签存在但缺陷已不可见 | 增强后可见性检查，训练最后 30% 的计划 epoch 关闭 Mosaic/强增强 |
| Copy-Paste 产生伪影 | 模型学习硬边、光晕或合成纹理 | CP-0→CP-4 递进，保存可视化样本，只在真实验证集提升时晋级 |
| 只切图丢失长裂纹 | 裂纹被切成多个短框 | 同权重全局视图 + 重叠切片 |
| 黑边产生误检 | 板边/黑区出现高分框 | ROI 掩膜、边缘 hard negative、反射填充 |
| CLAHE 放大噪声 | 麻坑/氧化铁皮误报增加 | 原图主干 + 门控残差辅助，不直接替换原图 |
| MixStyle 收益不确定 | ID 提升但 Style-OOD 不升，或不超过简单灰度增强 | 与亮度/对比度/Gamma/噪声做公平对照，按多 seed 决定去留 |
| 随机划分泄漏 | 验证分数虚高，榜单下降 | 按板材/批次前缀 Group Split，并保存划分版本 |
| 模块堆叠无法答辩 | 创新动机不清、贡献不可证 | 每次只增加一个主要因素，组合前先证明独立有效 |
| 测试域适应触碰规则 | 方案合规性不明确 | 不用测试伪标签，不做测试时训练，只做训练域泛化 |
| 推理太慢 | 每张图约 12 切片 + 1 全局，超出限制 | 先测端到端；ROI、batch inference、FP16、TensorRT，最后才换 s 模型 |
| 公告未定义完整指标 | 不同 IoU、P/R 权重和阈值实现会改变模型排序 | 启动时冻结版本化本地协议，同时报告完整 AP、普通类 F1、致命类 F2、Style-OOD 与 FP/image；公告若正式补充再新增兼容评估，不覆盖历史结果 |

---

## 14. 强基线与候选最终配置

### 14.1 可独立提交的强基线

这份配置不依赖任何尚未验证的自研模块，应该最早跑通并作为所有消融的共同基准：

```yaml
model: SteelGuard-YOLO11m-P2
weights: strong_baseline.pt
input:
  grayscale: repeat_to_3_channels
  tile_size: 1280
  tile_overlap: 256
  stride: 1024
  global_long_side: 1536
  roi_mask: true
network:
  p2_head: true
  aafm: false
  mffm: false
loss:
  cls: standard_or_verified_class_balanced_loss
  box: ciou
training:
  group_split: true
  rare_class_sampling: true
  basic_gray_domain_aug: true
  residual_copy_paste: false
  mixstyle: false
  train_only_domain_randomization: true
inference:
  one_model: true
  global_and_local_same_weights: true
  nms: verified_rule_compatible_setting
  per_class_thresholds: rule_dependent
output: submission.json
```

### 14.2 候选最终配置

最终权重只从强基线出发加入通过准入条件的模块；以下是配置结构，不代表所有值预先固定为 `true`：

```yaml
model: SteelGuard-YOLO11m-P2
weights: one_final_weight.pt
input:
  grayscale: repeat_to_3_channels
  tile_size: 1280
  tile_overlap: 256
  stride: 1024
  global_long_side: 1536
  roi_mask: true
network:
  p2_head: true
  aafm: selected_by_ablation
  mffm: selected_by_ablation
loss:
  cls: selected_verified_classification_loss
  box: selected_verified_box_loss
training:
  group_split: true
  rare_class_sampling: selected_by_ablation
  copy_paste: selected_cp_stage_or_false
  mixstyle: selected_by_ablation
  train_only_domain_randomization: true
inference:
  one_model: true
  global_and_local_same_weights: true
  postprocess: selected_rule_compatible_nms_or_wbf
  per_class_thresholds: selected_after_rule_confirmation
output: submission.json
```

`selected_by_ablation` 的含义是：模块必须在真实 ID/Style-OOD、关键类别、稳定性和复杂度约束下通过验证；没有稳定收益的模块明确关闭，而不是为了保留创新点强行启用。

---

## 15. 可用于技术报告的创新点表述

> 本方案针对超高分辨率板材图像中“微小缺陷易丢失、强伪影易误检、致命长尾缺陷易漏检、跨产线风格易偏移”四个工业痛点，提出单模型 SteelGuard-YOLO。首先构建 ROI 感知的全局—局部双尺度检测机制，在保留原始微小纹理的同时引入全局板面上下文；随后将 AAFM 抗伪影门控、MFFM 形态融合、长尾学习、可控 Copy-Paste 和训练域风格泛化作为候选模块，分别通过真实 ID 与 Style-OOD 消融验证其贡献，而不是预先假设全部模块同时有效。最终方案仅使用一个检测网络和一份模型权重，选择“强基线 + 被稳定证明有效且复杂度可接受的模块”，在规则允许的前提下兼顾精度、召回、泛化与工程成本。

---

## 16. 代码实现规范

以下规范适用于本项目所有代码文件（Python 脚本、配置生成、数据处理、训练、推理、评估等），AI 辅助编码时须逐条遵守。

### 16.1 路径管理：严禁硬编码绝对路径

**核心原则：所有路径必须相对于项目根目录，禁止在任何代码中使用绝对路径。**

- 项目根目录通过 `PROJECT_ROOT` 变量统一管理，利用 `pathlib.Path(__file__).resolve().parents[N]` 动态推导，不允许写死 `D:\Desktop\比赛` 或其他绝对路径；
- 数据目录由配置文件 `config.yaml` 或 `config.py` 中的 `data_root` 字段指定，默认值使用 `PROJECT_ROOT / "训练数据集"` 这样的相对形式；
- 推理和输出路径同样从配置读取，默认值为 `PROJECT_ROOT / "outputs"` 等；
- 所有 I/O 操作只接受 `Path` 对象或基于 `PROJECT_ROOT` 拼接的路径，不使用硬编码字符串路径；
- 示例：

  ```python
  # ✅ 正确
  from pathlib import Path

  PROJECT_ROOT = Path(__file__).resolve().parents[1]
  DATA_ROOT = PROJECT_ROOT / "训练数据集"
  TEST_ROOT = PROJECT_ROOT / "初赛测试集"
  OUTPUT_ROOT = PROJECT_ROOT / "outputs"

  # ❌ 错误
  DATA_ROOT = "D:/Desktop/比赛/训练数据集"
  img = cv2.imread("D:\\Desktop\\比赛\\data\\train\\001.jpg")
  ```

### 16.2 配置集中管理

- 所有超参数、路径、训练设置统一存放在 `configs/` 目录下的 YAML 或 Python 配置文件中；
- 代码文件不直接包含可调参数，通过 `--config` 命令行参数或配置类加载；
- 实验记录时自动保存本次运行的完整配置快照，确保可复现；
- 不同实验（强基线、AAFM、MFFM 等）使用独立配置文件，继承公共基础配置，避免拷贝粘贴。

### 16.3 环境与依赖

- 使用 `requirements.txt` 或 `environment.yaml` 锁定依赖版本；
- 在项目根目录提供 `environment.yaml`（conda）或 `pyproject.toml`/`requirements.txt`（pip）；
- GPU/CPU 环境自动检测，代码不得硬编码 `cuda:0` 等设备字符串，使用 `torch.cuda.is_available()` 判断；
- 训练与推理脚本启动时打印完整环境信息（Python、PyTorch、CUDA 版本、GPU 型号、驱动版本）。

### 16.4 代码风格

- 遵循 PEP 8，使用 4 空格缩进；
- 所有函数和类方法必须包含 docstring（Google 或 NumPy 风格），说明参数、返回值和功能；
- 变量/函数使用小写下划线 `snake_case`，类使用大驼峰 `PascalCase`；
- 类型注解：所有公共函数必须标注参数类型和返回值类型；
- 每个 `.py` 文件包含模块级 docstring，说明该模块在方案中的对应角色（如"对应方案第 5.2.1 节 AAFM 实现"）。

### 16.5 数据加载与转换

- VOC XML 解析使用统一的 `VOCDataset` 或 `SteelDataset` 类，不分散在多处重复实现；
- 标注转换脚本（VOC → YOLO 格式等）只依赖配置中的数据路径，不使用绝对路径；
- 数据增强使用 `albumentations` 或 torchvision 的 pipeline，以配置文件驱动，不在代码中硬编码增强参数；
- 所有随机操作（数据划分、增强采样、shuffle）必须设置并记录随机种子。

### 16.6 训练与验证

- 训练脚本通过命令行参数 `--config` 指定配置文件路径（相对路径）；
- Checkpoint 自动保存到配置指定的输出目录，命名包含 epoch、step 和关键指标；
- 验证/测试集划分以 JSON/YAML 文件持久化，按 stem 或文件名前部标识分组，代码中不能临时划分；
- 早停和学习率调度器参数从配置文件读取，不在代码中硬编码。

### 16.7 推理与输出

- 推理脚本接受 `--config`、`--weights`（相对路径或相对于 `PROJECT_ROOT` 的路径）和 `--input`（测试图片目录，相对路径）；
- 输出 JSON 保存到配置指定的路径，结构严格遵循赛题提交格式；
- 坐标映射、裁剪、去重等后处理逻辑封装为独立函数，每个函数有明确输入输出类型注解。

### 16.8 日志与可复现

- 使用 Python `logging` 模块，同时输出到控制台和文件，日志级别从配置读取；
- 每次实验自动生成带时间戳的运行目录，包含：配置文件副本、完整训练日志、checkpoint、评估结果 JSON；
- Git 提交前确保代码可从头训练复现，不依赖开发者本机特定路径或手动操作。

---

## 17. 结论

本题最值得投入的方向不是盲目更换更大的基础模型，而是：

1. **先用高分辨率切片和 P2 头解决“看不见”问题；**
2. **再用全局视图解决长裂纹被切断和上下文缺失；**
3. **用 ROI、抗伪影门控和硬负样本解决“看错”问题；**
4. **用长尾采样、NWD 与规则驱动的后处理解决致命缺陷“漏检”问题；**
5. **用训练域风格泛化和严格分组验证解决跨产线下降问题；**
6. **用统一的复杂度、稳定性和多 seed 证据决定 AAFM、MFFM、Copy-Paste、MixStyle 等候选模块是否晋级。**

方案不以固定日历时间提前砍掉候选方向，而是按前置条件、实验结果和资源状态自动推进；但最终提交仍不应默认全部叠加。推荐先完成可独立提交的强基线，再按分层实验验证每个模块，最后只保留在 ID、Style-OOD、关键类别和复杂度上都成立的配置。这样既能形成稳定成绩，也能确保技术报告中的每个创新点都有真实实验支撑。
